# Valores de DEV prontos pra usar com o docker-compose.yml da raiz (Postgres local).
# Em prod isso é reescrito com os valores reais (Neon, secret novo, etc). Só copiar:
#   cp backend/.env.example backend/.env
DATABASE_URL=postgresql+psycopg2://classpulse:classpulse@localhost:5432/classpulse
JWT_SECRET=LbMvHu-InH4eNRWxeTP8d7U3D5zqyASJS_3PO7F0jBM
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30
REFRESH_COOKIE_SECURE=True
